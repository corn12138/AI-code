# ====================================================================
# AI-Code 测试编排器最小依赖包
# 兼容 Python 3.11+ (包括 Python 3.13)
# 仅包含核心功能所需的必要依赖，已验证兼容性
# ====================================================================

# 核心必需依赖 (运行测试编排器的最小要求)
typer[all]==0.12.5              # CLI 框架（包含 rich 支持）
rich==13.7.1                   # 丰富的控制台输出
pyyaml==6.0.1                  # YAML 配置文件支持
psutil==5.9.8                  # 系统和进程监控
aiohttp==3.9.1                 # 异步 HTTP 客户端（健康检查）
jinja2==3.1.3                  # 模板引擎（报告生成）

# Git 集成
GitPython==3.1.41             # Git 操作和差异检测

# 文件监控（watch 模式）
watchdog==4.0.0               # 文件系统监控

# 日志系统
loguru==0.7.2                 # 高级日志库

# 实用工具
tqdm==4.66.2                  # 进度条
python-dateutil==2.8.2        # 日期时间处理

# ====================================================================
# 快速安装命令
# ====================================================================
# pip install -r requirements-minimal.txt
# 
# 这些包已在 Python 3.13 上验证通过
# 如需完整功能请安装 requirements.txt（部分包可能需要注释）
# ====================================================================