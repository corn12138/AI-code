# 📱 Mobile 应用

一个基于React + SSR的移动端技术文章阅读平台，支持服务端渲染、流式输出和优化的移动端体验。

> 📚 **完整文档**: 查看 [docs/README.md](./docs/README.md) 获取详细的技术文档和指南

## 🚀 特性

- **服务端渲染 (SSR)**: 提供更好的SEO和首屏加载性能
- **移动端优化**: 专为移动设备设计的响应式界面
- **流式输出**: 支持流式HTML输出，提升用户体验
- **数据预加载**: 服务端预加载数据，减少客户端请求
- **分类浏览**: 支持前端、后端、AI、移动端、设计等分类
- **文章详情**: 支持富文本内容展示
- **性能优化**: 代码分割、懒加载、缓存策略

## 📱 技术栈

### 前端
- **React 18**: 使用最新的React特性
- **TypeScript**: 类型安全的开发体验
- **Tailwind CSS**: 实用优先的CSS框架
- **Zustand**: 轻量级状态管理
- **React Router**: 客户端路由
- **Lucide React**: 现代化图标库

### 后端/SSR
- **Express**: Node.js服务器框架
- **React DOM Server**: 服务端渲染
- **Axios**: HTTP客户端
- **Compression**: Gzip压缩
- **CORS**: 跨域资源共享

### 构建工具
- **Vite**: 快速的构建工具
- **TypeScript**: 类型检查
- **ESLint**: 代码质量检查
- **Vitest**: 单元测试

## 🛠️ 开发指南

### 环境要求
- Node.js >= 16
- pnpm >= 7

### 安装依赖
```bash
pnpm install
```

### 开发模式

#### 客户端开发
```bash
pnpm dev
```
访问: http://localhost:3002

#### SSR开发
```bash
pnpm dev:ssr
```
访问: http://localhost:3000

### 构建部署

#### 构建客户端
```bash
pnpm build
```

#### 构建SSR
```bash
pnpm build:ssr
```

#### 生产环境预览
```bash
pnpm preview:ssr
```

## 📁 项目结构

```
apps/lowcode/
├── src/                    # 客户端源码
│   ├── components/         # React组件
│   │   ├── Article/        # 文章相关组件
│   │   └── Layout/         # 布局组件
│   ├── pages/              # 页面组件
│   ├── store/              # 状态管理
│   ├── types/              # TypeScript类型定义
│   ├── App.tsx             # 应用根组件
│   └── main.tsx            # 客户端入口
├── server/                 # SSR服务器
│   ├── api.ts              # API代理层
│   ├── entry-server.tsx    # SSR入口
│   ├── index.ts            # Express服务器
│   ├── store-server.ts     # 服务端状态管理
│   └── template.html       # HTML模板
├── public/                 # 静态资源
├── package.json            # 项目配置
├── vite.config.ts          # Vite配置
└── README.md               # 项目文档
```

## 🔧 配置说明

### 环境变量
- `PORT`: SSR服务器端口 (默认: 3000)
- `NODE_ENV`: 运行环境 (development/production)
- `API_BASE_URL`: 后端API地址 (默认: http://localhost:3001)

### API集成
项目支持与后端API集成，会自动从以下端点获取数据：
- `GET /api/articles`: 获取文章列表
- `GET /api/articles/:id`: 获取文章详情

如果API不可用，会自动降级到模拟数据。

## 🚀 部署

### Docker部署
```bash
# 构建镜像
docker build -t mobile-articles .

# 运行容器
docker run -p 3000:3000 -e API_BASE_URL=http://your-api-server mobile-articles
```

### 传统部署
```bash
# 构建项目
pnpm build:ssr

# 启动生产服务器
NODE_ENV=production node server/index.js
```

## 📊 性能优化

- **代码分割**: 按路由和功能模块分割代码
- **懒加载**: 图片和组件懒加载
- **缓存策略**: 静态资源和API响应缓存
- **压缩**: Gzip压缩和资源压缩
- **预加载**: 关键资源预加载
- **SSR**: 服务端渲染提升首屏性能

## 🧪 测试

```bash
# 运行单元测试
pnpm test

# 运行测试并监听变化
pnpm test:watch

# 生成测试覆盖率报告
pnpm coverage
```

## 📝 开发规范

- 使用TypeScript进行类型安全开发
- 遵循ESLint代码规范
- 组件使用函数式组件和Hooks
- 样式使用Tailwind CSS
- 提交前运行lint检查

## 🤝 贡献指南

1. Fork项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开Pull Request

## 📄 许可证

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。
